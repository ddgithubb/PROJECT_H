import { Dimensions } from "react-native";

export const PIXEL_WIDTH = Dimensions.get("window").width;
export const PIXEL_HEIGHT =  Dimensions.get("window").height;
export const HALF_PIXEL_WIDTH = PIXEL_WIDTH / 2;
